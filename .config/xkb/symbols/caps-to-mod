// Check with 'xmodmap -pm'
// This changes the key functions to Super_L, does not move the Caps_Lock to from lock to modifiers
xkb_symbols {
    // Caps_Lock to Super_L
    replace key <CAPS> { [ Super_L ] };
    // Menu to Super_L
    replace key <COMP> { [ Super_L ] };
};
